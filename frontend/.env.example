# Server-side rewrite target for Next.js deployments (proxy mode).
API_BASE_URL=http://127.0.0.1:8000
# Optional client-side backend URL for static/frontend-only deployments.
# Example: NEXT_PUBLIC_API_BASE_URL=https://your-api.example.com
NEXT_PUBLIC_API_BASE_URL=
