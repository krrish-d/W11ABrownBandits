# Server-side only — used by next.config.mjs rewrites to proxy /api/* to the backend.
# Do NOT use the NEXT_PUBLIC_ prefix here; browser code always routes through /api (the proxy).
API_BASE_URL=http://127.0.0.1:8000
